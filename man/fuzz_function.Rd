% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fuzz_function.R
\name{fuzz_function}
\alias{fuzz_function}
\title{Fuzz a single function}
\usage{
fuzz_function(
  fn,
  corpus = NULL,
  ...,
  input = c("raw", "string"),
  expect = character(),
  instrument = NULL,
  harness_out = NULL
)
}
\arguments{
\item{fn}{The function to fuzz. A function from a package namespace is
referred to by name and re-resolved in the child; any other closure is
serialized.}

\item{corpus}{Corpus directory.}

\item{...}{Passed to \code{\link[=fuzz_file]{fuzz_file()}}.}

\item{input}{\code{"raw"} passes the bytes; \code{"string"} passes text decoded from
them.}

\item{expect}{Condition classes \code{fn} may legitimately raise for bad input.
Caught narrowly around the call. Generic classes are rejected.}

\item{instrument}{Package to instrument; defaults to \code{fn}'s own package.}

\item{harness_out}{Where to write the generated harness. A temporary file
by default.}
}
\value{
A \code{zufuzz_result}, with the harness path in \verb{$harness}.
}
\description{
Generates a harness around \code{fn} and runs it through \code{\link[=fuzz_file]{fuzz_file()}}. The
generated script is kept, and its path reported, so a one-liner that finds
something can be promoted into a real harness.
}
