% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minimize.R
\name{minimize}
\alias{minimize}
\title{Shrink a finding to a smaller input that fails the same way}
\usage{
minimize(
  harness,
  finding,
  out = NULL,
  runs = 1000,
  accelerate = TRUE,
  timeout = 60
)
}
\arguments{
\item{harness}{Path to the harness that produced the finding.}

\item{finding}{Path to the artifact.}

\item{out}{Where to write the minimized artifact. Defaults to a file beside
\code{finding} with a \code{.min} suffix.}

\item{runs}{Maximum number of candidates to try. Each one is a fresh
process, so this is the real cost control.}

\item{accelerate}{Use \code{afl-tmin} first when it is available, then finish
with the gated reducer. The accelerator is never trusted on its own.}

\item{timeout}{Seconds allowed for each candidate.}
}
\value{
A \code{zufuzz_minimize}.
}
\description{
Each candidate is re-run through \code{\link[=replay]{replay()}} in a fresh process and kept
only if it reproduces the \emph{same} fingerprint. A candidate that fails
differently is rejected, however much smaller it is.
}
