% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fdp.R
\name{fuzzed_data_provider}
\alias{fuzzed_data_provider}
\title{Turn fuzzed bytes into typed values}
\usage{
fuzzed_data_provider(data)
}
\arguments{
\item{data}{A raw vector.}
}
\value{
A \code{zufuzz_fdp} object; call its methods with \code{$}.
}
\description{
A harness receives one raw vector. Most targets want more than one value
out of it, and splitting bytes by hand is where harnesses go wrong. This
mirrors LLVM's \code{FuzzedDataProvider}, so corpora and habits carry over from
C++ and from Atheris.
}
\details{
Two guarantees hold for every method:
\itemize{
\item \strong{Nothing errors, on any byte sequence.} Fuzzed input is adversarial by
definition; a provider that threw would report a defect in the harness as
a defect in the target.
\item \strong{Exhaustion is a value, not a failure.} Past the end of the input,
methods return zero-length or zero-ish results and the cursor stops.
}

Bytes are taken from the front and integers from the back, as LLVM does, so
the two never overlap.
}
\examples{
fdp <- fuzzed_data_provider(as.raw(c(1, 2, 3, 4, 5, 6, 7, 8)))
fdp$consume_bytes(2)
fdp$consume_int_in_range(1, 10)
fdp$remaining_bytes()
}
