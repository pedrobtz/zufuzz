% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objects.R
\name{draw}
\alias{draw}
\title{Generate objects from a spec, without an engine}
\usage{
draw(spec, n = 1L, seed = NULL, bytes = NULL)
}
\arguments{
\item{spec}{An \code{\link[=r_object]{r_object()}} spec.}

\item{n}{How many objects to draw.}

\item{seed}{Integer. Expanded to bytes through an internal PRNG that never
touches \code{.Random.seed}, so drawing is reproducible without disturbing the
caller's random stream.}

\item{bytes}{Raw vector to draw from instead of a seed. This is how an
exact object is reproduced.}
}
\value{
A list of objects, each carrying the bytes that produced it in a
\code{zufuzz_bytes} attribute.
}
\description{
The same machinery a campaign uses, driven by hand. \code{draw()} needs no
engine, no worker and no instrumentation, which is what makes it usable for
exploring a spec before committing a campaign to it.
}
\examples{
spec <- r_object(types = c("integer", "logical"), max_len = 4)
xs <- draw(spec, n = 3, seed = 42)
str(xs)
}
