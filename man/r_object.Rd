% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objects.R
\name{r_object}
\alias{r_object}
\title{Describe the objects a campaign should generate}
\usage{
r_object(
  types = c("logical", "integer", "double", "character", "list"),
  max_len = 16L,
  max_depth = 3L,
  validity = c("nasty", "strict"),
  names = TRUE
)
}
\arguments{
\item{types}{Which types may be generated.}

\item{max_len}{Longest vector or list to build.}

\item{max_depth}{How deep lists may nest.}

\item{validity}{\code{"strict"} builds only what base constructors produce.
\code{"nasty"} adds the values that are representable and legal but that most
code forgets: \code{NA} of every type, \code{NaN}, \verb{±Inf}, \code{-0}, zero-length
vectors, names, encoding marks, and both ALTREP and materialized forms of
the same value. A crash at either level is a defect.}

\item{names}{Whether generated vectors and lists may carry names.}
}
\value{
An \code{r_object_spec}.
}
\description{
A declarative spec rather than a generator closure, so it can be printed,
compared, and recorded in a finding's metadata -- which is what makes a
crash report say what kind of object caused it.
}
\examples{
spec <- r_object(types = c("integer", "character"), max_len = 8)
draw(spec, n = 2, seed = 1)
}
