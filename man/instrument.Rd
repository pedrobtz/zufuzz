% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instrument.R
\name{instrument}
\alias{instrument}
\alias{instrument_package}
\alias{instrument_all}
\title{Instrument selected functions for coverage feedback}
\usage{
instrument(..., compare = TRUE)

instrument_package(
  pkg,
  exclude = character(),
  recursive = FALSE,
  compare = TRUE
)

instrument_all(exclude = character(), include_base = FALSE, compare = TRUE)
}
\arguments{
\item{...}{Selections, as strings: \code{"pkg::fn"}, \code{"pkg:::internal"}, or the
name of a function in the calling environment.}

\item{compare}{Reserved for comparison tracing, which arrives with the
engine that can forward it. Accepted now so harnesses need not change.}

\item{pkg}{Package name.}

\item{exclude}{Function names to leave alone, bare or qualified.}

\item{recursive}{Also instrument the package's \code{Imports} and \code{Depends}.
This is the analogue of Atheris's \code{instrument_imports()}.}

\item{include_base}{Also instrument base and recommended packages. Off by
default: the noise and the cost are rarely worth it.}
}
\value{
The \code{\link[=instrumentation_report]{instrumentation_report()}}, invisibly.
}
\description{
Rewrites the body of each selected closure so that reaching a branch
records a hit, then replaces the binding it came from. The rewrite
preserves value, visibility, laziness, evaluation order, error propagation,
\code{return()}/\code{break}/\code{next}, and \code{on.exit()}.
}
\details{
Instrumentation must be complete before a campaign starts: attaching an
engine freezes the counter region, and a later call is an error rather than
a silent no-op.

All of these are no-ops when \code{ZUFUZZ_NO_INSTRUMENT} is set, which is how an
artifact is re-run against uninstrumented code.
}
\examples{
f <- function(x) if (x > 0) "positive" else "negative"
instrument("f")
f(1)
instrumentation_report()
}
